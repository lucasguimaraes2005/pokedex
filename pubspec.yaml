name: pokedex
description: Uma Pokédex pessoal usando Flutter e PokéAPI

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.3.2
  sqflite: ^2.3.0
  provider: ^6.0.5
  path: ^1.8.3
  cached_network_image: ^3.3.0
  cupertino_icons: ^1.0.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true